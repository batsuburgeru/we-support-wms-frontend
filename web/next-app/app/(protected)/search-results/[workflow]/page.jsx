import React from 'react';
import ProductSearchResults from '@/components/ProductSearchResults';

const SearchResults = () => {
  
  // Serves as the page to render the search results when searching for a product during a purchase requisition
  return (
    <main>
      <ProductSearchResults />
    </main>
  );
};

export default SearchResults;
