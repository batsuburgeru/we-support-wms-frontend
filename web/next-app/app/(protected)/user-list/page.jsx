import React from 'react';
import UserListPage from '@/components/UserListPage';

const UserList = () => {

  // Serves as the page that renders the User List page component
  return (
    <UserListPage />
  )
}

export default UserList